services:
  - type: web
    name: shopscanner
    runtime: node
    plan: starter
    buildCommand: npm install --omit=dev
    startCommand: npm start
    envVars:
      - key: SCANNER_PASSWORD
        sync: false
      - key: USE_PROXY
        value: "false"
